# Demo artwork

`open-source-demo-background.png` is an original abstract demo wallpaper generated for this repository. It contains no characters, trademarks, logos, or text and may be replaced with an image you are allowed to use.

The picture is duplicated under `dream-skin/assets/` because the runtime requires each built-in theme image to live next to its `theme.json`.
